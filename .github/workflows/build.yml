name: Build

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3.1.0
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v3.5.1
        with:
          node-version: '16'
          cache: 'pnpm'

      - run: pnpm install
      - run: pnpm build
      - run: pnpm test
